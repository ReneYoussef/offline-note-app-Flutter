name: offline_note_app
description: "A new Flutter project."

publish_to: 'none' 


version: 1.0.0+1

environment:
  sdk: ^3.9.2


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  drift_flutter: ^0.2.7
  drift: ^2.29.0
  path_provider: ^2.1.5
  sqlite3_flutter_libs: ^0.5.40
  intl: ^0.19.0
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7
  shared_preferences: ^2.5.3
  flutter_dotenv: ^5.1.0
  http: ^1.2.2
  shimmer: ^3.0.0



dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  drift_dev: ^2.29.0
  build_runner: ^2.10.1
  

flutter:

  uses-material-design: true

  assets:
    - .env
